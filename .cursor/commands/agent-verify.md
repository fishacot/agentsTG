---
description: Проверки проекта или CursoRules (markdown)
---

# Agent verify

1. **Код приложения:** команды из `docs/PROJECT_VERIFICATION.md` в **корне workspace**.
2. **Только правки в CursoRules (md/mdc):** если в `CursoRules/` есть `package.json` — `npm install` и `npm run verify` там; иначе ручной осмотр.

Канон: `CursoRules/AGENTS.md` → Verification.
