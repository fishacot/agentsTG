# Поведение агентов — как устроено сейчас

> Краткий справочник после рефакторинга goal-oriented (2026-05-28).

## Принцип

Пользователь пишет **обычным языком**. Агент:

1. Читает SOUL (личность) + память о пользователе.
2. Понимает **цель** сообщения.
3. Отвечает сам **или** вызывает инструмент (заметка, поиск, запомнить факт).

Нет форматов `CREATE_NOTE:`, `ACTION:`, `SEARCH:` в промпте.

## Память

| Уровень | Где | Что хранит |
|---------|-----|------------|
| **Личная (user_id)** | `memory_service` + PG `user_facts` | Факты через `remember_about_user` |
| **Mem0 (облако)** | если `MEM0_API_KEY` | Семантический поиск |
| **Fallback (VPS)** | in-process `_facts_store` | Последние ~100 фактов на user_id |
| **Журнал** | `_journal_store` + `workspace/.../JOURNAL.md` | Manus-style события |
| **История чата** | `chat_history` + PG | Последние turns per agent |
| **Workspace** | `workspace/users/{id}/` | USER.md, MEMORY.md, daily logs, HEARTBEAT.md |

Все агенты видят **одну память пользователя** (по `user_id` из Telegram).  
Агенты **не** делят отдельные «личные» базы — только общий контекст на человека.

Сохранение: модель вызывает `remember_about_user`, когда пользователь **сообщает факт** («меня зовут…», «я работаю…»). Вопрос «ты помнишь?» — **не** сохранение.

## Кто за что отвечает

| Агент | Бот | Действия (инструменты) | Разговор |
|-------|-----|------------------------|----------|
| **Егор** | @egor_orchestrator_bot | Маршрутизация, direct reply | Привет, «кто ты», координация |
| **Эльза** | @elliza_pa_bot | Заметки, задачи | Организация, память, small talk |
| **Руслан** | @ruslan_coder_bot | web_search | Код, архитектура |
| **Ульяна** | @ulyana_research_bot | web_search | Ресерч, ссылки |
| **Артём** | @artem_security_bot | web_search | Безопасность |
| **Ваня** | @aivanya_business_bot | web_search | Бизнес, MVP |
| **Тася** | @tasya_marketing_bot | web_search | Маркетинг |

## Егор vs специалисты

- Пишете **Егору** → он сам отвечает на приветствия; на задачи делегирует (без плана на «привет»).
- Пишете **напрямую** боту специалиста → отвечает только он, без Егора.

## Ограничения (пока)

- **Mem0 на VPS** без ключа — локальный fallback + PG facts (если Neon подключён).
- **Obsidian** — нужен vault + git на сервере, иначе заметки только локально в папке.
- **Calendar tool** — не реализован (backlog).
- **Proactive opt-out** per user — только глобальный `HEARTBEAT_ENABLED`.

## Связанные файлы

- Личности (черновик для правок): `docs/AGENT_PERSONALITIES.md`
- SOUL в коде: `src/agents_tg/agents/souls/*.md`
- Движок: `src/agents_tg/services/agent_runner.py`
